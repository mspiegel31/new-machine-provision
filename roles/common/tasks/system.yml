---
- name: install git
  become: true
  apt:
    name: git
- name: install zsh
  become: true
  apt:
    name: zsh

# - name: Install a list of packages
#   apt:
#     name: "{{ packages }}"
#   vars:
#     packages:
#     - foo
#     - foo-tools
